#include <stdio.h>
int main(void)
{
	int no;

	printf("2자리 정수를 입력하세요\n");
	do {
		printf("수는:");
		scanf("%d", &no);

	} while (no < 10 || no>99); // 논리합을 구하는 연산자 || 사용
		printf("변수 no의 값은 %d가 되었습니다.\n",no);
	return 0;

}
